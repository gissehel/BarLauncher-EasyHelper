name: build and publish

on:
    push:
        branches:
            - '*'
        tags:
            - 'v*'

jobs:
    build:
        name: Build and deploy
        runs-on: windows-latest
        steps:
            - uses: actions/checkout@v2

            - name: Get the version (for tags)
              if: "contains(github.ref, 'refs/tags')"
              shell: bash
              run: echo "VERSION=$(cat VERSION)" >> "${GITHUB_ENV}"

            - name: Get the version (for normal commits)
              if: "!contains(github.ref, 'refs/tags')"
              shell: bash
              run: echo "VERSION=$(cat VERSION)-$(date +%s)" >> "${GITHUB_ENV}"

            - name: pack BarLauncher-EasyHelper (version ${{ env.VERSION }})
              run: dotnet pack BarLauncher-EasyHelper.sln -c Release -p:VERSION=${{ env.VERSION }}

            - name: Upload Artifacts
              uses: actions/upload-artifact@v2
              with:
                  name: nupkg
                  path: ./nupkg/*.nupkg

            - name: Publish to api.nuget.org
              if: "contains(github.ref, 'refs/tags')"
              run: nuget push **\*.nupkg -Source 'https://api.nuget.org/v3/index.json' -ApiKey ${{secrets.NUGET_API_KEY}}
